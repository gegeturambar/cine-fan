{% extends 'base.html.twig' %}

{% block body %}
    <div class="row marketing jumbotron">
        <div class="col-lg-8">
            <h1 class="display-3">{{ movie.title }}</h1>
            <p class="lead">{{ movie.description }}</p>

            <p>
                <label>Date de sortie : </label> {{ movie.releaseDate | date('d/m/Y') }}</p>

            <p>
                <label>Catégorie : </label> {{ movie.category.name }}

            </p>

        </div>
        <div class="col-lg-3">
            <img width="300" height="300" src="/upload/movie/{{ movie.picture }}"/>
        </div>
    </div>


{% endblock %}

{% block stylesheets %}

{% endblock %}
