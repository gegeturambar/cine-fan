<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><a href="{{ url('app.homepage.index') }}">Accueil</a></li>
                <li class=""><a href="{{ url('app.main.listcateg') }}">Recherche par catégorie</a></li>
                <li class=""><a href="{{ url('app.main.search') }}">Chercher des films</a></li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ url('app.admin.category.form') }}">Ajouter une catégorie</a></li>
                        <li><a href="{{ url('app.admin.movie.form') }}">Ajouter un film</a></li>
                    </ul>
                </li>

            </ul>
            <ul class="nav navbar-nav navbar-right">
                {#si identifié#}
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li><a href="#">Films en base : {{ nb_films.allMovies | raw }}</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>